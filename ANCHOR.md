# ANCHOR · 《别绷了》项目记忆锚点

> 早安。这一个是项目的全部状态、我拍板的每个方向决策及理由、验证记录。
> 游戏在线：**https://xiaoyuanbenchu.github.io/biebengle/** （已验证 v3.0 线上可玩）
> 本地：双击 `index.html`，或 `node serve.js` 按打印的局域网地址手机打开。

---

## 一句话

梗式搞笑抽象的一键反应小游戏：彩圈缩向白圈，重合瞬间按，按歪绷死一条🤡命。零依赖单文件网页 + PWA。

## 当前状态：v3.0 已上线（2026-09-06 凌晨完成并验证）

- 版本演进：v1 单文件原型 → v2 圈型深度+成就+每日挑战+PWA+公网部署 → **v3 当前**
- 仓库：github.com/xiaoyuanbenchu/biebengle（GitHub Pages，push main 即自动发布）
- 手机旧缓存更新机制：sw.js 的 CACHE 版本号已升到 `biebengle-v3`，老用户第二次打开自动换新

## v3.0 相比 v2 新增

1. **玩法**
   - 新圈型「反向圈」（40 分后出没）：从小长大撞向白圈，旋转方向反转作视觉提示
   - 「👑 绷王时刻」Boss 战：每跨 50 分触发，连续 3 个超窄窗橘红圈，每个 Perfect +6 分，3 连全中解锁成就「绷王认证」，中途失手即结束
   - 险过提示：按歪但离判定窗很近时吐槽「就差一点！」而不是随机梗
   - 出生速度随机抖动（±10%），防背板节奏
2. **生涯系统**
   - 成就 8 → 16 个（新增：夜猫子绷手/百分俱乐部/一日三绷/金卷王/丝滑天胡/绷学奇才/每日打卡/绷王认证）
   - 累计绷力 = 生涯总分，对应段位称号（摸鱼选手 → 绷学奇才 十级），主页和结算页都展示
   - 主页新增「🏆 战绩」页：生涯六项数据 + 16 成就墙（🔒/🏆）
   - 老玩家迁移：首次载入 v3 时把 v2 的历史最高分带进生涯数据
3. **视听**
   - WebAudio 五声音阶背景音乐：随连击变密变亮（15 连后升八度），低音脉搏打底，极小音量，右上角一键全静音；`MUSIC_ON` 常量可整体关闭
   - 星空漂移背景、彩圈速度方向拖尾、画面暗角、菜单页氛围旋转环、10 连后 HUD 连击火焰脉动
4. **工程**
   - sw.js 缓存版本 → v3；favicon/og 分享卡片标签；测试钩子模式下跳过 SW 注册（避免自动化测试被缓存干扰）

## 文件地图

| 文件 | 作用 |
|---|---|
| `game.js` / `game.json` / `project.config.json` / `js/core.js` | **小游戏工程（必须在仓库根目录，开发者工具从根目录导入）**。core.js = 整个游戏，双端运行（真机 wx.* / 浏览器原生）。改了游戏只动 core.js |
| `index.html` / `sw.js` / `manifest.webmanifest` / `icon-512.png` | 网页版（GitHub Pages 同域共存，互不干扰） |
| `icon.html` | 图标源文件（headless Edge 截图生成 PNG） |
| `serve.js` | 局域网试玩服务器（8934，含路径穿越防护与 MIME 表） |
| `test/harness.html` + `test/wx-shim.js` | 浏览器测试通道。**shim 绝不能定义 window.wx**（否则 core 的 isWx 误判，输入桥和钩子全失效） |
| `ANCHOR.md` | 本文件——项目唯一记忆锚点 |
| `shots/` | 回归截图（git 忽略） |

**开发者工具导入步骤**：微信开发者工具 → 导入 → 目录选 `X:\量化课改\bie-beng-le`（根目录！）→ AppID 自动读到 wx7c6ab4289784e091 → 类型应显示「小游戏」。**若提示 AppID 与项目类型不匹配**（个人主体注册的是小程序 AppID，非小游戏 AppID）：点导入框里的「测试号」即可正常开发+真机预览；体验版上传才必须小游戏类目 AppID（个体户，见 Phase 2）。

## 本地存档（localStorage 键）

`biebengle_best` 最高分 · `biebengle_achv` 成就 · `biebengle_seen` 圈型介绍 · `biebengle_daily_<日期>` 每日最佳 · `biebengle_stats` 生涯数据

## 测试钩子（URL hash）

`#autotest` 自动开局强制命中 · `#autolose` 自动送命看结算 · `#autodaily` 每日挑战自动局 · `#autostats` 直接开战绩页。钩子模式下 SW 不注册。

## 决策日志（自主拍板 + 理由）

- **反向圈而非幽灵圈**：幽灵圈（消失判定）在手机小屏是挫败感制造机；反向圈保持公平，只改预判方向
- **Boss = 窄窗金圈×3 而非血条**：单按钮游戏的 Boss 应是极限手感时刻，不是数值堆砌
- **成就带正有负**（夜猫子、一日三绷这种"行为梗"）：抽象游戏的趣味在吐槽玩家
- **背景音乐默认开但极小声**：audio 无法远程验证，选择保守参数 + `MUSIC_ON` 开关兜底，不好听一键可关
- **不做排行榜**：需要后端+账号，破坏"发链接即玩"的零摩擦传播，进 backlog
- **不做皮肤商店**：纯装饰对留存帮助小于玩法深度
- **每日种子取本地日期**：跨时区不同关，接受（全球同关需 UTC+展示倒计时，过度设计）

## 调试实录（给未来的自己）

- headless 验证务必用**全新 `--user-data-dir`**：旧 profile 的 Service Worker 会端出缓存旧页，且 SW 与虚拟时间机制相性差
- 严格模式下漏删一个变量赋值（`missRun=0`）让 `startGame` 全灭——`new Function` 语法检查抓不到运行时 ReferenceError，**要配合 `--enable-logging=stderr` 抓 CONSOLE 错误 + `--dump-dom` 查 DOM 断言**
- git bash 里 curl -d 直接带中文 JSON 会编码坏掉，ASCII 化或写文件再 `-d @file`

## 验证记录（v3.0，全部通过）

1. `new Function` 语法检查 ✓
2. 本地截图矩阵：菜单（新按钮/成就 0/16/生涯行/v3.0 标识）✓ · 对局（82 分/手指成精/32 连/UFO/拖尾/星空/看穿你了）✓ · 绷死结算（生涯行/夜猫子成就实证触发）✓ · 战绩页（16 成就墙+六项数据）✓
3. 线上：v3.0 标记轮询到位 ✓ · sw.js 200 ✓ · 线上对局截图（90 分/绷学奇才/金圈）✓

## 真机调试实录（重要经验，2026-09-06）

- **真机现象演进**：v0(原始)=能玩但缩圈不显示（元凶①：`hsl(H S% L%)` 空格语法老 canvas 不认）→ 改逗号 hsl 后=缩圈只剩右侧小碎片（元凶②：**canvas arc() 角度用 Date.now() 级大数（28 亿弧度）时真机角度精度爆炸，画成碎片**；PC/headless 用小虚拟时间戳永远复现不了）→ 定案修复：**动画时钟用帧累加小数值（`_ts += dt`，与系统时钟解耦）+ arc 角度对 2π 取模 + 颜色全 hex（hsl2hex 转换器在 core.js 顶部）**
- **性能教训**：`shadowBlur` 在真机原生 canvas 是填充率杀手（中端机 1-2fps），已全换双层描边假辉光；DPR 封顶 2 会糊字，去掉 shadow 后恢复 3 不卡
- **教训总纲**：PC/headless 正常 ≠ 真机正常。真机三坑：①canvas 颜色/角度解析保守 ②填充率敏感 ③时钟量纲不一。**动画一律用帧累加时钟 + 小数值 + hex 颜色，别碰系统时钟大数**
- 排查真机问题的正道：**开发者工具「真机调试」**（PC 实时看手机 console），比截图猜快得多；预览版 vConsole 绿色按钮是备用
- **真机调试日志分析结论（2026-09-06，Android + 基础库 3.17.2）**：用户全流程走通，日志中 0 条游戏代码报错。`getSystemInfo jsbridge not ready`（框架启动噪音）、`DETECT WARN no-parallel-download`（性能优化建议非错误）、`webapi_game_transfer -31001`（微信托管能力开发环境不可用，框架级）均为框架内部噪音。Android 原生 canvas 的保守行为（hsl 不认、大角度精度崩）与排查推断吻合
- 服务器进程会被会话回收，headless 测试前先 curl 探活 127.0.0.1:8934

## 路线图（backlog，按价值排序）

排行榜（CloudBase 云函数可零服务器实现）→ 音量分离设置（音乐/音效分开）→ 新圈型池扩充 → 皮肤系统 → 多语言

---

# 微信小游戏计划（MP-1，2026-09-06 与用户共同确认）

> 实验命题（用户原话）：「测试自然语言交互编程能否上线一款封装的完整游戏」。
> Phase 1 产出体验版 = 第一份证据；Phase 2 正式过审上线 = 完整结论。

## 已确认的关键约束

- **个人主体不能发小游戏类目**（只对企业/个体工商户开放）→ 体验版先行，正式上线需注册个体工商户
- 无内购休闲小游戏上线需**软著**、不需版号；变现走激励视频广告（流量主门槛 1000 UV）
- **开放数据域好友真榜 = 游戏类目专属**，体验版阶段用「挑战卡片+复仇模式」替代，Phase 2 无缝升级

## 用户已拍板

体验版先行（暂不注册个体户）· 目标=好友比拼+公开传播+学习全流程+验证交互编程实验 · 策略=移植+微信化改造 · 视觉=程序生成升级版（零外部素材）· 全球榜=上云开发 · 节奏=阶段连续推进 · AppID=用户现在去注册个人号

## 阶段与验收门

- **阶段 A · 玩法迁移 ✅ 已完成（2026-09-06）**：`minigame/` 工程骨架（game.js/game.json/project.config.json 已配 AppID）+ `js/core.js` 全 canvas 迁移。验收=headless harness 全场景可玩 ✓
- **阶段 B · 微信化+视觉 ✅ 已完成（2026-09-06）**：遗言系统（按死法自动生成+换一句/改一句 wx.showKeyboard）、血仇链（query 传递 c=N）、复仇模式（横幅/胜利演出/反击卡/失败可再战）、挑战卡片（分数段文案+段位前缀+canvas 生成分享图）、绷狂 60 秒（无限命计时刷分）、迷走圈（30 分后白圈游走）、4 皮肤（霓虹/熔岩/像素/幽蓝，成就解锁）、成就 16→19。验收=分享→复仇闭环 headless 走通 ✓
- **阶段 C · 云开发全球榜 ❌ 用户裁决砍掉（2026-09-06）**：「先上线收反馈，不想花钱」——零成本策略。若未来要做，方案仍在此节历史里
- **阶段 D · 上线（当前阶段）双轨**：
  - **轨道 1 · 网页版反馈通道（已上线✅）**：index.html 已替换为小游戏同款 core.js 全功能版（复仇遗言/血仇链/绷狂60秒/迷走圈/皮肤/19成就），分享走 navigator.share→剪贴板兜底，**分享链接自带 `?t=rev&c=N&s=S&m=遗言` 参数，网页上血仇链照样传染**（index.html 内联 bootstrap 解析 search+hash）。SW 缓存 v4。链接：https://xiaoyuanbenchu.github.io/biebengle/ ——这就是收集用户反馈的主阵地，把链接发出去即可
  - **轨道 2 · 小游戏正式上架（Phase 2 清单，需用户线下办理）**：注册个体工商户（各地免费~几百元，1-2 周）→ 以个体户主体注册**小游戏**账号（个人主体无法，墙已实证）→ 微信认证 300 元/年 → 软著自办（官方免费，1-2 月，与前面并行）→ 提审上线 → 1000 UV 后开激励视频广告。总成本约 300-600 元、周期 3-6 周
  - 里程碑意义：用户实验命题「自然语言交互编程上线完整游戏」的"上线"以轨道 1 即日达成，轨道 2 为正式形态

## 双端架构（重要）

`minigame/js/core.js` 一份代码跑两端：真机走 `wx.*`；浏览器走原生（localStorage/AudioContext/pointer/prompt）。**test/wx-shim.js 绝不能定义 window.wx**——否则 core 的 isWx 探测误判，输入桥和测试钩子全部失效（已踩过此坑）。测试参数用 hash 传：`#hook名&k=v`。

## MP 测试钩子（harness URL hash）

`#autotest` 强制命中 · `#autolose` 送命看遗言结算 · `#autodaily` 每日自动局 · `#autosprint` 60秒模式 · `#autorev` 复仇胜利全链路（先命中超分再送命触发演出）· `#autorevlose` 复仇失败 · `#autostats` 战绩页。线上 harness：https://xiaoyuanbenchu.github.io/biebengle/test/harness.html （手机浏览器也能玩，等于免费的手游 H5 版）

## MP 验证记录（全部通过，0 未捕获异常）

菜单（标题/皮肤解锁态/成就 8/19 递增）· 对局（58 分/金圈/迷走圈横幅）· 绷死结算（遗言 UI）· 战绩页（19 成就墙+复仇胜场）· 绷狂60秒（⏱ 计时 HUD）· 复仇胜利（横幅→送命→碎片演出）· 复仇失败（200 分不可达目标）。浏览器 AudioContext/vibrate 的手势警告为 headless 环境预期行为，真机无此限制。

## Phase 2 清单（用户决定公开上线时启动）

注册个体工商户（几百元/1-2 周）→ 小游戏类目+微信认证（300 元）→ 软著申请（并行，正常 1-2 月可加急）→ 开放数据域真好友榜 → 提审过审 → 1000 UV 开流量主激励视频

## 待用户输入

1. ~~个人 AppID~~ → **已提供：wx7c6ab4289784e091**（2026-09-06，个人主体，类目占位「工具」；AppSecret 绝不能进仓库）
2. 用户的想法与思路 → 头脑风暴进行中，已确认：复仇模式带**遗言**（死亡场景自动生成+可编辑，挂卡片/对局顶部/反击卡三处）、复仇成功有**特效演出+反击分享**；挑战卡片文案风格交给我；小程序名交给我；玩法新增走逐项评估
3. 阶段 C 到达时：确认云开发付费意愿

## 头脑风暴候选池（v4 玩法，待用户逐项裁决）

- ✅ 建议做：限时模式「绷狂 60 秒」（无限命刷分，双榜第二支柱）、迷走圈（30 分后白圈小范围游走）、轻量皮肤（3-5 个程序生成皮肤成就解锁）、卡片带段位称号、连击飘字倍率、Haptic 编排（perfect/golden/boss/复仇四档震动）
- 🤔 缓：双子圈（双白圈轮流有效）、黄金一瞬（随机 0.3s 三倍窗）、异步双人对战（云开发房间）
- ❌ 不做：幽灵圈（挫败感）、禅绷模式（低价值）、遗言图鉴（过度设计）
- 复仇链套娃概念：卡片显示「第 N 手血仇」（纯参数实现，传播梗强）

